# Symphony Broad Smoke

This PR intentionally adds a compact feature-flag evaluator with several reviewable edge cases.

It is only here to exercise Symphony's pull request review path across multiple files and issue types.
